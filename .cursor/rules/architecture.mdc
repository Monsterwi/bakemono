---
description: 架构级规则 - 指导高性能HTTP缓存代理服务器的构建
alwaysApply: true
---

# Bakemono 架构设计规则

本文档定义了基于Golang的高性能HTTP缓存代理服务器的架构级设计规则，参考Apache Traffic Server的设计理念。

## 1. 项目目标与设计原则

### 1.1 核心目标
- **高性能**: 高吞吐量、低延迟，支持大规模并发请求
- **高可用**: 容错设计，单点故障不影响整体服务
- **可扩展**: 支持水平扩展，易于添加新功能
- **轻量级**: 代码简洁清晰，易于理解和维护

### 1.2 设计原则
- **分层架构**: 清晰的层次划分，各层职责明确
- **异步优先**: 优先使用异步I/O，避免阻塞操作
- **零拷贝**: 在可能的情况下使用零拷贝技术减少内存开销
- **缓存友好**: 数据结构设计考虑CPU缓存行对齐
- **故障隔离**: 单个请求失败不应影响其他请求

## 2. 架构层次

### 2.1 整体架构层次
```
┌─────────────────────────────────────┐
│   HTTP Server Layer (Gin/Net/HTTP) │
├─────────────────────────────────────┤
│   Engine Layer (请求路由与协调)      │
├─────────────────────────────────────┤
│   Cache Layer (多级缓存系统)         │
│   ├─ RAM Cache (L1)                 │
│   ├─ Write Buffer (L2)              │
│   └─ Disk Cache (L3)                │
├─────────────────────────────────────┤
│   Proxy Layer (反向代理与负载均衡)   │
├─────────────────────────────────────┤
│   Storage Layer (持久化存储)         │
│   ├─ Volume                         │
│   ├─ Stripe                         │
│   └─ Chunk                          │
└─────────────────────────────────────┘
```

### 2.2 组件职责划分

#### Engine Layer
- **职责**: 请求入口、路由分发、缓存协调
- **关键组件**: `Engine`, `CacheKey`, `ConsistentHash`
- **设计要点**:
  - 使用singleflight防止缓存击穿
  - 使用一致性哈希分布缓存到不同Volume
  - 快速路径优化：缓存命中直接返回

#### Cache Layer
- **职责**: 多级缓存管理、缓存策略执行
- **关键组件**: `RamCache`, `Stripe`, `DirManager`
- **设计要点**:
  - L1: RAM缓存使用LRU策略，快速响应热点数据
  - L2: 聚合写入缓冲区，减少磁盘I/O
  - L3: 磁盘持久化存储，支持大规模数据
  - 读取路径: L1 → L2 → L3 三级查找
  - 写入路径: 先写L2缓冲区，异步刷盘

#### Storage Layer
- **职责**: 数据持久化、元数据管理
- **关键组件**: `Vol`, `Stripe`, `Chunk`, `Dir`
- **设计要点**:
  - Volume: 单个磁盘文件，支持多Volume分布
  - Stripe: 逻辑分区，减少锁竞争
  - Chunk: 数据存储基本单元，包含元数据和数据
  - Dir: 内存中的元数据索引，紧凑设计节省内存

#### Proxy Layer
- **职责**: 反向代理、负载均衡、健康检查
- **关键组件**: `HTTPProxy`, `Balancer`, `HealthCheck`
- **设计要点**:
  - 支持多种负载均衡算法（轮询、一致性哈希等）
  - 自动健康检查，故障自动剔除
  - 连接池复用，减少连接开销

## 3. 缓存系统设计规范

### 3.1 缓存键设计
- **规则**: 缓存键必须唯一标识可缓存的内容
- **实现**: 使用URL路径+特定查询参数生成缓存键
- **示例**: `GetCacheKey()` 函数应包含路径和关键查询参数
- **注意**: 避免包含会话ID、时间戳等动态参数

### 3.2 缓存一致性
- **分布策略**: 使用一致性哈希将缓存分布到多个Volume
- **哈希算法**: 使用稳定的哈希算法（如MD5），确保相同键映射到相同Volume
- **容错**: Volume故障时，应能自动从其他Volume或上游获取数据

### 3.3 缓存替换策略
- **RAM缓存**: 使用Otter,内置S3-FIFO淘汰策略
- **磁盘缓存**: 使用循环覆盖策略，避免碎片化
- **淘汰机制**: 当空间不足时，优先淘汰冷数据

### 3.4 缓存失效
- **TTL支持**: 支持基于时间的缓存失效（未来实现）
- **手动失效**: 提供API支持手动清除缓存（未来实现）
- **当前策略**: 基于磁盘空间循环覆盖

## 4. 存储系统设计规范

### 4.1 Volume设计
- **文件大小**: 支持大文件（TB级别），使用固定大小预分配
- **并发控制**: 使用Stripe分区减少锁竞争
- **元数据持久化**: 定期刷新元数据到磁盘，支持崩溃恢复

### 4.2 Stripe设计
- **分区策略**: 基于key的CRC32哈希值分配到不同Stripe
- **独立资源**: 每个Stripe拥有独立的锁、目录管理器、写入缓冲区
- **负载均衡**: Stripe数量应合理，避免过多导致资源浪费，过少导致锁竞争

### 4.3 Chunk设计
- **固定头部**: 4KB固定头部，对齐磁盘扇区
- **数据校验**: 使用校验和确保数据完整性
- **键存储**: 完整键存储在Chunk头部，用于碰撞检测

### 4.4 Dir设计
- **内存紧凑**: 使用位操作压缩元数据，每个Dir仅10字节
- **索引结构**: 使用Segment-Bucket组织，支持快速查找
- **空闲管理**: 使用freelist管理空闲Dir，支持快速分配

## 5. 代理系统设计规范

### 5.1 负载均衡
- **算法支持**: 支持轮询、一致性哈希、最少连接等算法
- **权重支持**: 支持后端服务器权重配置（未来实现）
- **会话保持**: 支持基于IP或Cookie的会话保持（未来实现）

### 5.2 健康检查
- **检查方式**: TCP连接检查 + HTTP健康检查（未来实现）
- **检查间隔**: 可配置的检查间隔，默认3秒
- **故障处理**: 自动剔除故障节点，恢复后自动加入

### 5.3 连接管理
- **连接池**: 使用HTTP连接池复用连接
- **超时控制**: 设置合理的连接超时、读取超时、写入超时
- **限流保护**: 支持最大连接数限制，防止过载

## 6. 并发与性能优化

### 6.1 并发模型
- **Goroutine**: 充分利用Goroutine处理并发请求
- **锁粒度**: 使用细粒度锁（Stripe级别），避免全局锁
- **无锁数据结构**: 在可能的情况下使用无锁数据结构（如atomic操作）

### 6.2 性能优化原则
- **零拷贝**: 使用`io.Copy`等零拷贝技术传输数据
- **批量操作**: 聚合写入缓冲区，批量刷盘
- **预分配**: 预分配内存和文件空间，减少动态分配
- **缓存对齐**: 关键数据结构考虑CPU缓存行对齐

### 6.3 异步I/O
- **写入路径**: 使用异步写入，不阻塞请求处理
- **读取路径**: 使用异步读取，支持流式传输
- **元数据刷新**: 后台定期刷新元数据，不影响请求处理

## 7. 错误处理与容错

### 7.1 错误分类
- **可恢复错误**: 磁盘I/O错误、网络超时等，应重试或降级
- **不可恢复错误**: 配置错误、内存不足等，应记录日志并拒绝请求
- **缓存错误**: 缓存读取失败应视为MISS，从上游获取

### 7.2 容错策略
- **降级策略**: 缓存故障时直接透传到上游
- **超时控制**: 所有外部调用都应设置超时
- **熔断机制**: 上游持续故障时，应启用熔断（未来实现）

### 7.3 日志记录
- **分级日志**: 使用不同日志级别（DEBUG/INFO/WARN/ERROR）
- **结构化日志**: 使用结构化日志（如zap），便于分析
- **性能日志**: 记录关键操作的耗时，便于性能分析

## 8. 代码组织规范

### 8.1 包结构
```
bakemono/
├── engine.go          # Engine核心逻辑
├── cache_key.go       # 缓存键生成
├── vol.go             # Volume管理
├── stripe.go          # Stripe管理
├── chunk.go           # Chunk操作
├── dir.go             # Dir元数据
├── ram_cache.go       # RAM缓存
├── upstream.go        # 上游代理
├── config.go          # 配置管理
├── logger.go          # 日志管理
└── balancer/          # 负载均衡算法
```

### 8.2 命名规范
- **接口**: 使用动词命名，如`Reader`, `Writer`
- **结构体**: 使用名词命名，如`Engine`, `Volume`
- **函数**: 使用动词开头，如`Get`, `Set`, `Init`
- **常量**: 使用大写字母和下划线，如`StripeBlockSize`

### 8.3 接口设计
- **最小接口**: 接口应尽可能小，遵循接口隔离原则
- **依赖注入**: 使用依赖注入提高可测试性
- **错误返回**: 使用Go标准错误处理，返回`error`类型

## 9. 测试要求

### 9.1 单元测试
- **覆盖率**: 核心功能单元测试覆盖率应≥80%
- **边界测试**: 测试边界条件和异常情况
- **并发测试**: 使用`go test -race`检测竞态条件

### 9.2 集成测试
- **端到端测试**: 测试完整的请求处理流程
- **性能测试**: 使用benchmark测试关键路径性能
- **压力测试**: 测试高并发场景下的系统表现

### 9.3 测试数据
- **真实数据**: 使用接近生产环境的测试数据
- **大数据量**: 测试TB级别数据的处理能力
- **故障模拟**: 模拟磁盘故障、网络故障等场景

## 10. 参考Traffic Server的最佳实践

### 10.1 架构借鉴
- **事件驱动**: 参考ATS的事件驱动架构，使用Goroutine实现类似效果
- **多级缓存**: 借鉴ATS的RAM+磁盘多级缓存设计
- **元数据设计**: 参考ATS的Dir紧凑设计，节省内存

### 10.2 性能优化借鉴
- **零拷贝**: 学习ATS的零拷贝技术
- **批量操作**: 参考ATS的批量写入策略
- **缓存友好**: 学习ATS的CPU缓存友好设计

### 10.3 功能扩展方向
- **HTTP/2支持**: 未来支持HTTP/2协议
- **插件系统**: 参考ATS的插件架构，支持功能扩展
- **管理API**: 提供类似ATS的管理接口，支持运行时配置

## 11. 配置管理

### 11.1 配置文件
- **格式**: 使用YAML格式，易于阅读和修改
- **验证**: 启动时验证配置有效性
- **热重载**: 未来支持配置热重载（不重启服务）

### 11.2 关键配置项
- **存储配置**: Volume路径、大小、数量
- **缓存配置**: RAM缓存大小、平均Chunk大小
- **代理配置**: 上游服务器、负载均衡算法
- **性能配置**: 并发数、超时时间、缓冲区大小

## 12. 监控与可观测性

### 12.1 指标收集
- **缓存指标**: 命中率、未命中率、缓存大小
- **性能指标**: 请求延迟、吞吐量、错误率
- **系统指标**: CPU、内存、磁盘I/O使用率

### 12.2 日志输出
- **访问日志**: 记录所有请求的详细信息
- **错误日志**: 记录错误和异常情况
- **性能日志**: 记录慢请求和性能瓶颈

### 12.3 健康检查
- **健康端点**: 提供HTTP健康检查端点
- **就绪检查**: 区分健康检查和就绪检查（未来实现）
- **存活检查**: 定期检查系统关键组件状态

## 13. 安全考虑

### 13.1 输入验证
- **请求验证**: 验证请求参数，防止注入攻击
- **路径遍历**: 防止路径遍历攻击
- **大小限制**: 限制请求体和响应体大小

### 13.2 访问控制
- **IP白名单**: 支持IP白名单/黑名单（未来实现）
- **认证授权**: 支持API认证（未来实现）
- **速率限制**: 支持请求速率限制（未来实现）

### 13.3 数据安全
- **传输加密**: 支持HTTPS/TLS加密传输
- **数据校验**: 使用校验和确保数据完整性
- **敏感信息**: 不在日志中记录敏感信息

## 14. 开发流程

### 14.1 代码审查
- **审查要点**: 性能影响、错误处理、测试覆盖
- **审查工具**: 使用golangci-lint等工具自动检查

### 14.2 文档要求
- **代码注释**: 关键函数和结构体应有详细注释
- **设计文档**: 重大功能变更应有设计文档
- **API文档**: 公共API应有使用示例

### 14.3 版本管理
- **语义化版本**: 遵循语义化版本规范
- **变更日志**: 维护详细的变更日志
- **向后兼容**: 尽量保持API向后兼容

---

**注意**: 本规则文档是活文档，应根据项目发展持续更新和完善。在开发过程中遇到的新问题和新需求，应及时补充到本文档中。
